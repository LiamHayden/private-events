<%= render "/events/navbar" %>

<h1>Showing all events this user has created.</h1>
<% @user.created_events.each do | event | %>
  <p>Name: <%= event.name %> </p>
<% end %>

<h2>Event Attending</h2>
<% @user.attended_events.each do |event| %>
  <p>Event: <%= link_to event.name, event_path(event) %> </p>
<% end %>